#include <stdio.h>

/*
Definition:
heap is a type of tree data structure, nodes are required to be greater or less than or equal to its children

Task of implementation:
Insert
Extract min or max
Peek
Heapify
Delete
Increase or decrease key
Build Heap
*/

struct heap {
    int size;
    int capacity;
    int* arr;
};

int main(void){


    return 0;
}
