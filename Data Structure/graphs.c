#include <stdio.h>


// new idea: pointer to a pointer, but not enough knowledge yet to complete


struct Node {
    int vertex;
    struct Node* next;
};

struct Graph {
    int numVertices;
    struct Node** adjLists;
};


int main(void){



    return 0;
}
